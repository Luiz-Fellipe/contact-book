import React from 'react';

// STYLES
import { StyledHeader } from './styles';

export const Header: React.FC = () => (
  <StyledHeader>
    <div>
      <h1>Vexpenses Challenge</h1>
      <h2>Lista de Contatos</h2>
    </div>
  </StyledHeader>
);
